---
title: escape_once
description: escapes a string without changing existing escaped entities. It doesn’t change strings that don’t have anything to escape.
version: 1.9.1
works: true
---
{% assign formula = "1 < 2 & 3" | escape_once %}
{% render 'example-raw', number: 1, input: '{{ "1 < 2 & 3" | escape_once }}', output: formula %}

{% assign formula = "1 &lt; 2 &amp; 3" | escape_once %}
{% render 'example-raw', number: 2, input: '{{ "1 &lt; 2 &amp; 3" | escape_once }}', output: formula %}