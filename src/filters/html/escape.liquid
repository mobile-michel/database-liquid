---
title: escape
description: escapes a string by replacing HTML special characters with escape sequences. It doesn’t change strings that don’t have anything to escape.
version: 1.9.1
works: true
---
{% assign formula = "Have you read &#39;James &amp; the Giant Peach&#39;?" %}
{% render 'example-raw', number: 1, input: "{{ Have you read 'James & the Giant Peach'? | escape }}", output: formula %}
{% assign formula = "Tetsuro Takara" %}
{% render 'example-raw', number: 2, input: "{{ Tetsuro Takara | escape }}", output: formula %}